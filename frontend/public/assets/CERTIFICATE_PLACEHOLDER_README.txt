REQUIRED ACTION BEFORE DEPLOYING
=================================

No Udyam Registration Certificate image file was provided/uploaded
during the build of this project, so no certificate image has been
placed here. The certificate viewer component is fully built and
ready to display it — it just needs the real file.

To activate the "View Registration Certificate" feature:

1. Obtain the real Udyam Registration Certificate image/scan for
   Subramani Enterprises (JPG or PNG, as high resolution as possible).

2. Save it in this exact folder as:

       udyam-certificate.jpg

   (i.e. the final path must be:
    frontend/public/assets/udyam-certificate.jpg)

3. That's it — no code changes are needed. The CertificateViewer
   component already references this exact path.

Do NOT recreate, redraw, or fabricate the certificate. Use the
actual scanned/official document only.

Once the real file is in place, delete this placeholder text file.
